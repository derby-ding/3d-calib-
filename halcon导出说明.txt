首先将hdev程序调试通过，然后点击‘文件’下的‘导出’选项，选择导出的格式如C#,C++等，接着，将导出的函数体中的函数拷入VS程序的Main(作为类声明也可以，使用public声明函数)，别忘了添加Using HalconDotNet或者namespace HalconDotNet， 最后在main中添加调用入口

关于参数传递：halcon的hdev文件通常包含了完整的输入输出定义，而在C中，子函数更像是一个有输入和输出的控件，如何将hdev也变成C中的子函数呢？简单的方法是将参数随机赋值，然后导出C函数，改造此C函数，使其有输入和输出即可。